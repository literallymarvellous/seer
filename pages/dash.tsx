import { NextPage } from "next";
import { Dashboard } from "../components/Dashboard";

const Dash: NextPage = () => {
  return (
    // <Dashboard address={address} />
    <div>Dashboard</div>
  );
};

export default Dash;
